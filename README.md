# Dynamic-Matrix-project-c-
Uni project: A C/C++ program that performs matrix operations using dynamically allocated 2D arrays (int **) and advanced pointer arithmetic *(*(array + i) + j). It populates the matrix randomly and calculates row/column averages and diagonal sums.
